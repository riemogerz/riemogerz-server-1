// import app
const app = require('../app');

// definisikan port
const port = process.env.PORT || 3000;

// listen
app.listen(port, () => {
	console.log(`Example app listening on port ${port} test mode`)
})
